---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pom.xml
  - backend/src/main/resources/application.properties
  - backend/src/main/java/de/baier/familyadmin/config/SecurityConfig.java
  - backend/src/main/java/de/baier/familyadmin/config/JwtConfig.java
  - backend/src/main/java/de/baier/familyadmin/model/User.java
  - backend/src/main/java/de/baier/familyadmin/model/Role.java
  - backend/src/main/java/de/baier/familyadmin/repository/UserRepository.java
  - backend/src/main/java/de/baier/familyadmin/service/JwtService.java
  - backend/src/main/java/de/baier/familyadmin/service/UserService.java
  - backend/src/main/java/de/baier/familyadmin/controller/AuthController.java
  - backend/src/main/java/de/baier/familyadmin/dto/LoginRequest.java
  - backend/src/main/java/de/baier/familyadmin/dto/LoginResponse.java
  - backend/src/main/java/de/baier/familyadmin/dto/RegisterRequest.java
  - backend/src/main/java/de/baier/familyadmin/filter/JwtAuthFilter.java
  - backend/src/main/resources/db/migration/V1__create_users.sql
autonomous: true
requirements:
  - 5 individual family member accounts
  - Private per-person data foundation
  - Admin role can manage accounts

must_haves:
  truths:
    - POST /api/auth/login returns a JWT access token and sets httpOnly refresh cookie
    - POST /api/auth/refresh returns new access token using refresh cookie
    - GET /api/auth/me returns current user profile
    - Endpoints without valid JWT return 401
    - ADMIN role can call POST /api/admin/users to create accounts
    - User entity has visibility field supporting shared/private data scoping
  artifacts:
    - backend/src/main/java/de/baier/familyadmin/config/SecurityConfig.java
    - backend/src/main/java/de/baier/familyadmin/service/JwtService.java
    - backend/src/main/java/de/baier/familyadmin/controller/AuthController.java
    - backend/src/main/resources/db/migration/V1__create_users.sql
  key_links:
    - JwtAuthFilter reads token → JwtService validates → SecurityContext populated
    - SecurityConfig permits /api/auth/** and secures everything else
---

<objective>
Build the complete JWT authentication backend for FamilyAdmin.

Purpose: Auth is the foundation everything else depends on. Without it, no API is safe and no user context exists.
Output: Working login/refresh/logout endpoints, JWT filter, User model with ADMIN/MEMBER roles, Flyway migration, CORS configured for Next.js frontend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/AUTH.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace jjwt with built-in Nimbus (oauth2-resource-server)</name>
  <files>backend/pom.xml</files>
  <read_first>backend/pom.xml</read_first>
  <action>
    Remove the 3 jjwt dependencies (jjwt-api, jjwt-impl, jjwt-jackson) and replace with:
    ```xml
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
    </dependency>
    ```
    Also remove the jjwt.version property. Keep Flyway, Security, and all other dependencies.
  </action>
  <verify>grep -c "jjwt" backend/pom.xml — must return 0</verify>
  <acceptance_criteria>
    - backend/pom.xml contains 'oauth2-resource-server'
    - backend/pom.xml does NOT contain 'jjwt'
  </acceptance_criteria>
  <done>pom.xml uses Nimbus, no jjwt present</done>
</task>

<task type="auto">
  <name>Task 2: Flyway migration — create users table</name>
  <files>backend/src/main/resources/db/migration/V1__create_users.sql</files>
  <action>
    Create the migration file at `backend/src/main/resources/db/migration/V1__create_users.sql`:
    ```sql
    CREATE TABLE users (
        id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        email       VARCHAR(255) NOT NULL UNIQUE,
        name        VARCHAR(255) NOT NULL,
        password    VARCHAR(255) NOT NULL,
        role        VARCHAR(20)  NOT NULL DEFAULT 'MEMBER',
        refresh_jti VARCHAR(255),
        created_at  TIMESTAMPTZ  NOT NULL DEFAULT NOW(),
        updated_at  TIMESTAMPTZ  NOT NULL DEFAULT NOW()
    );

    CREATE INDEX idx_users_email ON users(email);
    ```
    This is the only table in V1. Subsequent modules add their own migrations.
  </action>
  <verify>cat backend/src/main/resources/db/migration/V1__create_users.sql | grep "CREATE TABLE users"</verify>
  <acceptance_criteria>
    - File exists at correct Flyway path
    - Contains UUID primary key, email unique constraint, role column, refresh_jti column
  </acceptance_criteria>
  <done>V1__create_users.sql exists and is valid SQL</done>
</task>

<task type="auto">
  <name>Task 3: User entity and Role enum</name>
  <files>
    backend/src/main/java/de/baier/familyadmin/model/User.java,
    backend/src/main/java/de/baier/familyadmin/model/Role.java
  </files>
  <action>
    Create Role.java:
    ```java
    package de.baier.familyadmin.model;
    public enum Role { ADMIN, MEMBER }
    ```

    Create User.java with Lombok, JPA, Spring Security UserDetails:
    - id: UUID
    - email: String (unique)
    - name: String
    - password: String (hashed)
    - role: Role (enum, stored as string)
    - refreshJti: String (nullable, for soft revocation)
    - createdAt, updatedAt: OffsetDateTime
    - Implement UserDetails — getAuthorities() returns role as GrantedAuthority
    - @Table(name = "users"), @EntityListeners(AuditingEntityListener.class)
    - Use Lombok @Data, @Builder, @NoArgsConstructor, @AllArgsConstructor
  </action>
  <verify>ls backend/src/main/java/de/baier/familyadmin/model/</verify>
  <acceptance_criteria>
    - User.java implements UserDetails
    - Role.java has ADMIN and MEMBER values
  </acceptance_criteria>
  <done>User and Role classes created</done>
</task>

<task type="auto">
  <name>Task 4: UserRepository and UserService</name>
  <files>
    backend/src/main/java/de/baier/familyadmin/repository/UserRepository.java,
    backend/src/main/java/de/baier/familyadmin/service/UserService.java
  </files>
  <action>
    Create UserRepository.java:
    ```java
    package de.baier.familyadmin.repository;
    public interface UserRepository extends JpaRepository<User, UUID> {
        Optional<User> findByEmail(String email);
        boolean existsByEmail(String email);
    }
    ```

    Create UserService.java implementing UserDetailsService:
    - loadUserByUsername(email) — used by Spring Security
    - createUser(name, email, rawPassword, role) — BCrypt password, save, return User
    - updateRefreshJti(userId, jti) — store or clear refresh token JTI
    - Annotate with @Service, @RequiredArgsConstructor (Lombok)
  </action>
  <verify>ls backend/src/main/java/de/baier/familyadmin/repository/ && ls backend/src/main/java/de/baier/familyadmin/service/</verify>
  <acceptance_criteria>
    - UserRepository has findByEmail method
    - UserService implements UserDetailsService
  </acceptance_criteria>
  <done>UserRepository and UserService created</done>
</task>

<task type="auto">
  <name>Task 5: JwtService</name>
  <files>backend/src/main/java/de/baier/familyadmin/service/JwtService.java</files>
  <action>
    Create JwtService.java using the Nimbus JOSE library (included via oauth2-resource-server):
    - generateAccessToken(User user) — HS256, 15 min expiry, claims: sub=email, role=role
    - generateRefreshToken() — random UUID string (stored as JTI in DB)
    - validateToken(token) — returns Optional<Claims>
    - extractEmail(token) — returns subject
    - Read secret from @Value("${app.jwt.secret}")
    - Read expiry from @Value("${app.jwt.expiration-ms}")
    - Annotate with @Service
  </action>
  <verify>grep "generateAccessToken\|validateToken" backend/src/main/java/de/baier/familyadmin/service/JwtService.java</verify>
  <acceptance_criteria>
    - JwtService has generateAccessToken, generateRefreshToken, validateToken methods
  </acceptance_criteria>
  <done>JwtService created with Nimbus</done>
</task>

<task type="auto">
  <name>Task 6: JWT auth filter</name>
  <files>backend/src/main/java/de/baier/familyadmin/filter/JwtAuthFilter.java</files>
  <action>
    Create JwtAuthFilter.java extending OncePerRequestFilter:
    - Extract Bearer token from Authorization header
    - Validate via JwtService
    - Load user via UserService.loadUserByUsername(email)
    - Set UsernamePasswordAuthenticationToken into SecurityContextHolder
    - Skip filter for paths matching /api/auth/**
    - Annotate with @Component
  </action>
  <verify>grep "OncePerRequestFilter" backend/src/main/java/de/baier/familyadmin/filter/JwtAuthFilter.java</verify>
  <acceptance_criteria>
    - JwtAuthFilter extends OncePerRequestFilter
    - Filter skips /api/auth/** paths
  </acceptance_criteria>
  <done>JwtAuthFilter created</done>
</task>

<task type="auto">
  <name>Task 7: Spring Security configuration</name>
  <files>backend/src/main/java/de/baier/familyadmin/config/SecurityConfig.java</files>
  <action>
    Create SecurityConfig.java with @Configuration @EnableWebSecurity @EnableMethodSecurity:
    - SecurityFilterChain bean using lambda DSL (NOT deprecated and() chaining)
    - Disable CSRF (SPA with httpOnly cookie handles this via SameSite=Strict)
    - CORS: allow origin http://localhost:3000 (dev) and ${app.frontend.url} (prod)
    - Permit: /api/auth/**, /actuator/health
    - Require authentication: all other /api/** endpoints
    - Add JwtAuthFilter before UsernamePasswordAuthenticationFilter
    - PasswordEncoder bean: BCryptPasswordEncoder
    - AuthenticationManager bean wired to UserService + PasswordEncoder
    - SecurityContextHolder strategy: MODE_INHERITABLETHREADLOCAL (virtual threads fix)
  </action>
  <verify>grep "EnableMethodSecurity\|MODE_INHERITABLETHREADLOCAL" backend/src/main/java/de/baier/familyadmin/config/SecurityConfig.java</verify>
  <acceptance_criteria>
    - SecurityConfig uses lambda DSL
    - @EnableMethodSecurity present
    - MODE_INHERITABLETHREADLOCAL set
    - CORS configured for localhost:3000
  </acceptance_criteria>
  <done>SecurityConfig created with all required settings</done>
</task>

<task type="auto">
  <name>Task 8: DTOs for auth endpoints</name>
  <files>
    backend/src/main/java/de/baier/familyadmin/dto/LoginRequest.java,
    backend/src/main/java/de/baier/familyadmin/dto/LoginResponse.java,
    backend/src/main/java/de/baier/familyadmin/dto/RegisterRequest.java,
    backend/src/main/java/de/baier/familyadmin/dto/UserResponse.java
  </files>
  <action>
    Create simple record-based DTOs:
    - LoginRequest: email (String), password (String)
    - LoginResponse: accessToken (String), user (UserResponse)
    - RegisterRequest: name (String), email (String), password (String)
    - UserResponse: id (UUID), name (String), email (String), role (Role)

    Use Java records (not classes) — clean and Lombok-free.
    Add @NotBlank validation annotations on LoginRequest and RegisterRequest fields.
  </action>
  <verify>ls backend/src/main/java/de/baier/familyadmin/dto/</verify>
  <acceptance_criteria>
    - 4 DTO files exist
    - DTOs are Java records
  </acceptance_criteria>
  <done>All DTOs created as Java records</done>
</task>

<task type="auto">
  <name>Task 9: AuthController</name>
  <files>backend/src/main/java/de/baier/familyadmin/controller/AuthController.java</files>
  <action>
    Create AuthController.java with @RestController @RequestMapping("/api/auth"):
    - POST /login — authenticate, generate tokens, return LoginResponse, set refresh token in httpOnly SameSite=Strict cookie (name: "refresh_token", maxAge: 7 days)
    - POST /refresh — read refresh cookie, validate JTI against DB, generate new access token + rotate refresh JTI
    - POST /logout — clear refresh cookie, clear JTI in DB
    - GET /me — return current user as UserResponse (@AuthenticationPrincipal User user)
    - Annotate with @RequiredArgsConstructor
  </action>
  <verify>grep "POST\|GetMapping\|RequestMapping" backend/src/main/java/de/baier/familyadmin/controller/AuthController.java</verify>
  <acceptance_criteria>
    - AuthController has /login, /refresh, /logout, /me endpoints
    - /login sets httpOnly cookie
  </acceptance_criteria>
  <done>AuthController with all 4 endpoints created</done>
</task>

<task type="auto">
  <name>Task 10: AdminController — create family accounts</name>
  <files>backend/src/main/java/de/baier/familyadmin/controller/AdminController.java</files>
  <action>
    Create AdminController.java with @RestController @RequestMapping("/api/admin"):
    - POST /users — create a new family member account
      - @PreAuthorize("hasRole('ADMIN')") — only Frank can create accounts
      - Body: RegisterRequest (name, email, password)
      - Calls UserService.createUser()
      - Returns 201 with UserResponse
    - GET /users — list all family members
      - @PreAuthorize("hasRole('ADMIN')")
      - Returns List<UserResponse>
  </action>
  <verify>grep "PreAuthorize\|ADMIN" backend/src/main/java/de/baier/familyadmin/controller/AdminController.java</verify>
  <acceptance_criteria>
    - @PreAuthorize("hasRole('ADMIN')") on all admin endpoints
    - POST /api/admin/users creates a user
  </acceptance_criteria>
  <done>AdminController with ADMIN-only endpoints created</done>
</task>

<task type="auto">
  <name>Task 11: Enable JPA Auditing</name>
  <files>backend/src/main/java/de/baier/familyadmin/FamilyAdminApplication.java</files>
  <read_first>backend/src/main/java/de/baier/familyadmin/FamilyAdminApplication.java</read_first>
  <action>
    Add @EnableJpaAuditing to FamilyAdminApplication.java so @CreatedDate / @LastModifiedDate work on entities.
  </action>
  <verify>grep "EnableJpaAuditing" backend/src/main/java/de/baier/familyadmin/FamilyAdminApplication.java</verify>
  <acceptance_criteria>
    - @EnableJpaAuditing present on FamilyAdminApplication
  </acceptance_criteria>
  <done>JPA Auditing enabled</done>
</task>

</tasks>
