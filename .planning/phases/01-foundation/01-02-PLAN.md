---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/app/layout.tsx
  - frontend/app/globals.css
  - frontend/app/(auth)/login/page.tsx
  - frontend/app/(app)/layout.tsx
  - frontend/app/(app)/dashboard/page.tsx
  - frontend/lib/api.ts
  - frontend/lib/auth.ts
  - frontend/components/nav/Sidebar.tsx
  - frontend/components/nav/UserMenu.tsx
  - frontend/middleware.ts
autonomous: true
requirements:
  - 5 individual family member accounts
  - Admin role can manage accounts

must_haves:
  truths:
    - Visiting /login shows the login form
    - Submitting valid credentials redirects to /dashboard
    - Invalid credentials show an error message
    - Visiting a protected route without auth redirects to /login
    - Sidebar shows navigation links for all 5 modules (Tasks, Recipes, Travel, Documents, + Dashboard)
    - User name and logout button visible in the app shell
  artifacts:
    - frontend/app/(auth)/login/page.tsx
    - frontend/app/(app)/layout.tsx
    - frontend/lib/auth.ts
    - frontend/middleware.ts
  key_links:
    - middleware.ts reads auth cookie → redirects unauthenticated users to /login
    - login page calls /api/auth/login → stores access token → redirects to /dashboard
---

<objective>
Build the Next.js 16 frontend auth flow and app shell.

Purpose: The login screen, protected route guard, and navigation shell are what the family sees first.
Output: Working login page, route protection via middleware, app shell with sidebar navigation for all modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: API client utility</name>
  <files>frontend/lib/api.ts</files>
  <action>
    Create frontend/lib/api.ts — a thin fetch wrapper:
    - Base URL from env: process.env.NEXT_PUBLIC_API_URL (default: http://localhost:8080)
    - apiFetch(path, options) — sets Content-Type: application/json, includes credentials: 'include' (for httpOnly cookie), adds Authorization: Bearer {token} header when token available
    - getAccessToken() / setAccessToken(token) / clearAccessToken() — store token in module-level variable (NOT localStorage — memory only as per AUTH.md)
    - Handle 401 responses: call /api/auth/refresh automatically, retry once
    Export: apiFetch, getAccessToken, setAccessToken, clearAccessToken
  </action>
  <verify>grep "credentials.*include\|NEXT_PUBLIC_API_URL" frontend/lib/api.ts</verify>
  <acceptance_criteria>
    - api.ts uses credentials: 'include'
    - Access token stored in memory, not localStorage
    - Auto-refresh on 401
  </acceptance_criteria>
  <done>api.ts created with token management and auto-refresh</done>
</task>

<task type="auto">
  <name>Task 2: Auth helpers</name>
  <files>frontend/lib/auth.ts</files>
  <action>
    Create frontend/lib/auth.ts:
    - login(email, password) — calls POST /api/auth/login, stores access token, returns user object
    - logout() — calls POST /api/auth/logout, clears access token
    - refreshToken() — calls POST /api/auth/refresh, updates stored access token
    - getCurrentUser() — calls GET /api/auth/me, returns user or null
    - Type: User { id: string, name: string, email: string, role: 'ADMIN' | 'MEMBER' }
  </action>
  <verify>grep "login\|logout\|getCurrentUser" frontend/lib/auth.ts</verify>
  <acceptance_criteria>
    - auth.ts exports login, logout, getCurrentUser, refreshToken
  </acceptance_criteria>
  <done>auth.ts created with all auth helpers</done>
</task>

<task type="auto">
  <name>Task 3: Login page</name>
  <files>frontend/app/(auth)/login/page.tsx</files>
  <action>
    Create the login page at frontend/app/(auth)/login/page.tsx.
    - 'use client' directive
    - Form with email and password fields
    - useActionState hook for form state (Next.js 16 native pattern — NOT react-hook-form)
    - On submit: call auth.login(), redirect to /dashboard on success
    - Show error message on failure ("Invalid email or password")
    - Style with Tailwind CSS — clean, centered card layout
    - Show "FamilyAdmin" heading and Baier family branding
    - No registration form (accounts created by admin only)
    Also create frontend/app/(auth)/layout.tsx — simple centered layout with no sidebar.
  </action>
  <verify>grep "useActionState\|login" frontend/app/\(auth\)/login/page.tsx</verify>
  <acceptance_criteria>
    - Login page uses useActionState (not useState + onSubmit)
    - Error state displayed on failed login
    - Redirects to /dashboard on success
  </acceptance_criteria>
  <done>Login page created with useActionState</done>
</task>

<task type="auto">
  <name>Task 4: Route protection middleware</name>
  <files>frontend/middleware.ts</files>
  <action>
    Create frontend/middleware.ts (Next.js 16 — note: 'middleware' is the correct export, NOT 'proxy'):
    - Protect all routes under / except /login
    - Check for auth_session cookie (set by backend on login) OR a custom x-auth-token header
    - If no auth: redirect to /login
    - If already authenticated and visiting /login: redirect to /dashboard
    - Export config matcher: ['/((?!_next/static|_next/image|favicon.ico).*)']
    IMPORTANT: Next.js 16 uses async Request APIs — use `await` on request.cookies, headers etc.
  </action>
  <verify>grep "middleware\|matcher" frontend/middleware.ts</verify>
  <acceptance_criteria>
    - middleware.ts exports middleware function and config with matcher
    - Unauthenticated requests to protected routes are redirected to /login
  </acceptance_criteria>
  <done>Route protection middleware created</done>
</task>

<task type="auto">
  <name>Task 5: App shell layout with sidebar</name>
  <files>
    frontend/app/(app)/layout.tsx,
    frontend/components/nav/Sidebar.tsx,
    frontend/components/nav/UserMenu.tsx
  </files>
  <action>
    Create the authenticated app shell:

    Sidebar.tsx — left navigation:
    - Links: Dashboard (/dashboard), Tasks (/tasks), Recipes (/recipes), Travel (/travel), Documents (/documents)
    - Active link highlighted
    - FamilyAdmin logo/name at top
    - UserMenu at bottom

    UserMenu.tsx — shows logged-in user's name and a logout button.
    - On logout: call auth.logout(), redirect to /login

    frontend/app/(app)/layout.tsx:
    - Flex layout: Sidebar (fixed left, full height) + main content area
    - Wrap with any necessary context providers
    - Tailwind CSS — clean, minimal sidebar with good contrast
  </action>
  <verify>grep "Tasks\|Recipes\|Travel\|Documents" frontend/components/nav/Sidebar.tsx</verify>
  <acceptance_criteria>
    - Sidebar has links for all 5 modules
    - UserMenu shows logout functionality
    - App layout renders sidebar + content area
  </acceptance_criteria>
  <done>App shell with sidebar created</done>
</task>

<task type="auto">
  <name>Task 6: Dashboard placeholder page</name>
  <files>frontend/app/(app)/dashboard/page.tsx</files>
  <action>
    Create a simple dashboard page at frontend/app/(app)/dashboard/page.tsx:
    - Display a welcome message: "Welcome to FamilyAdmin, [name]!"
    - Show placeholder cards for each module (Tasks, Recipes, Travel, Documents)
    - Each card has module name, brief description, and a "Coming soon" or link badge
    - Clean grid layout with Tailwind CSS
    - This is the landing page after login — make it feel welcoming
  </action>
  <verify>grep "Welcome\|dashboard" frontend/app/\(app\)/dashboard/page.tsx</verify>
  <acceptance_criteria>
    - Dashboard page exists at /dashboard
    - Shows welcome message and module cards
  </acceptance_criteria>
  <done>Dashboard placeholder page created</done>
</task>

<task type="auto">
  <name>Task 7: Update root page to redirect to dashboard</name>
  <files>frontend/app/page.tsx</files>
  <read_first>frontend/app/page.tsx</read_first>
  <action>
    Replace the default Next.js boilerplate in app/page.tsx with a simple redirect to /dashboard:
    ```tsx
    import { redirect } from 'next/navigation'
    export default function Home() {
      redirect('/dashboard')
    }
    ```
  </action>
  <verify>grep "redirect" frontend/app/page.tsx</verify>
  <acceptance_criteria>
    - app/page.tsx redirects to /dashboard
  </acceptance_criteria>
  <done>Root page redirects to /dashboard</done>
</task>

<task type="auto">
  <name>Task 8: Environment configuration</name>
  <files>frontend/.env.local.example</files>
  <action>
    Create frontend/.env.local.example (example file — NOT .env.local which is gitignored):
    ```
    NEXT_PUBLIC_API_URL=http://localhost:8080
    ```
    This documents the required environment variables for the frontend.
    Also update frontend/.gitignore to ensure .env.local is ignored (it likely already is from create-next-app).
  </action>
  <verify>cat frontend/.env.local.example</verify>
  <acceptance_criteria>
    - .env.local.example documents NEXT_PUBLIC_API_URL
  </acceptance_criteria>
  <done>Environment example file created</done>
</task>

</tasks>
