---
phase: 02-tasks
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/app/(app)/tasks/page.tsx
  - frontend/app/(app)/tasks/[id]/page.tsx
  - frontend/app/(app)/tasks/new/page.tsx
  - frontend/lib/tasks.ts
  - frontend/components/tasks/TaskCard.tsx
  - frontend/components/tasks/TaskList.tsx
  - frontend/components/tasks/TaskForm.tsx
  - frontend/components/tasks/ChecklistEditor.tsx
  - frontend/components/tasks/ChecklistItem.tsx
autonomous: true
requirements:
  - Create tasks with rich description
  - Checklist items within a task
  - Assign task to a specific family member
  - Due date on each task
  - View my tasks vs all family tasks
  - Tasks can be marked complete

must_haves:
  truths:
    - /tasks shows all family tasks and a "My Tasks" toggle
    - Clicking a task opens the task detail page
    - Task detail shows description, checklist items (checkable), assignee, due date
    - /tasks/new has a form to create a task with title, description, assignee, due date, checklist items
    - Checklist items can be added/removed in the form
    - Submitting the form creates the task and redirects to /tasks
    - Completing a task marks it visually as done
  artifacts:
    - frontend/app/(app)/tasks/page.tsx
    - frontend/app/(app)/tasks/[id]/page.tsx
    - frontend/lib/tasks.ts
    - frontend/components/tasks/TaskForm.tsx
  key_links:
    - tasks.ts API calls → /api/tasks backend
    - TaskForm uses useActionState for form state
    - ChecklistEditor manages dynamic checklist items in form
---

<objective>
Build the Tasks UI — task list, task detail, and create task form.

Purpose: This is the first module the Baier family will actually use daily.
Output: Full task management UI with family/mine toggle, task detail with checkable checklist, create form.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/FEATURES.md
@.planning/research/STACK.md
@frontend/lib/api.ts
@frontend/lib/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tasks API client</name>
  <files>frontend/lib/tasks.ts</files>
  <action>
    Create frontend/lib/tasks.ts with typed API functions:

    Types:
    - ChecklistItem: { id, text, done, position }
    - Task: { id, title, description, status, assignee: User|null, createdBy: User, dueDate: string|null, completedAt: string|null, checklistItems: ChecklistItem[], createdAt: string }
    - TaskRequest: { title, description?, assigneeId?, dueDate?, checklistItems?: {text, position}[] }

    Functions (all use apiFetch from lib/api.ts):
    - getTasks(): Promise<Task[]>
    - getMyTasks(): Promise<Task[]>
    - getTask(id: string): Promise<Task>
    - createTask(data: TaskRequest): Promise<Task>
    - updateTask(id: string, data: TaskRequest): Promise<Task>
    - completeTask(id: string): Promise<Task>
    - toggleChecklistItem(taskId: string, itemId: string): Promise<ChecklistItem>
    - deleteTask(id: string): Promise<void>
  </action>
  <verify>grep "getTasks\|createTask\|toggleChecklistItem" frontend/lib/tasks.ts</verify>
  <acceptance_criteria>
    - tasks.ts exports all 8 API functions with proper types
  </acceptance_criteria>
  <done>tasks.ts API client created</done>
</task>

<task type="auto">
  <name>Task 2: TaskCard component</name>
  <files>frontend/components/tasks/TaskCard.tsx</files>
  <action>
    Create TaskCard.tsx — a card displaying a task summary:
    - Title (bold, with strikethrough when status=DONE)
    - Status badge (OPEN=blue, IN_PROGRESS=yellow, DONE=green)
    - Assignee avatar (initials) and name, or "Unassigned"
    - Due date — show in red if overdue, orange if due today, grey otherwise
    - Checklist progress: "3/5 items" if task has checklist items
    - Click → navigate to /tasks/[id]
    - Props: task: Task, onClick?: () => void
    - Tailwind CSS — clean card with hover shadow, cursor-pointer
  </action>
  <verify>grep "TaskCard\|status\|dueDate" frontend/components/tasks/TaskCard.tsx</verify>
  <acceptance_criteria>
    - TaskCard shows status badge, assignee, due date, checklist progress
    - Overdue dates shown in red
  </acceptance_criteria>
  <done>TaskCard component created</done>
</task>

<task type="auto">
  <name>Task 3: ChecklistItem component</name>
  <files>frontend/components/tasks/ChecklistItem.tsx</files>
  <action>
    Create ChecklistItem.tsx — a single checkable item:
    - Checkbox (toggles done state via toggleChecklistItem API call)
    - Text (strikethrough when done)
    - Optimistic UI: toggle immediately, revert on error
    - Props: item: ChecklistItem, taskId: string, onToggle: (item: ChecklistItem) => void
    - 'use client' directive
  </action>
  <verify>grep "checkbox\|toggleChecklistItem\|strikethrough" frontend/components/tasks/ChecklistItem.tsx</verify>
  <acceptance_criteria>
    - ChecklistItem renders checkbox and text
    - Checked items show strikethrough text
  </acceptance_criteria>
  <done>ChecklistItem component created</done>
</task>

<task type="auto">
  <name>Task 4: ChecklistEditor component (for form)</name>
  <files>frontend/components/tasks/ChecklistEditor.tsx</files>
  <action>
    Create ChecklistEditor.tsx — dynamic checklist builder for the task form:
    - List of text inputs (one per checklist item)
    - "Add item" button appends a new empty input
    - "×" button removes an item
    - Enter key in an input adds a new item below
    - Props: items: string[], onChange: (items: string[]) => void
    - 'use client' directive
    - Clean Tailwind styling — focus on usability
  </action>
  <verify>grep "Add item\|onChange" frontend/components/tasks/ChecklistEditor.tsx</verify>
  <acceptance_criteria>
    - ChecklistEditor allows adding/removing checklist items
    - onChange called on every change
  </acceptance_criteria>
  <done>ChecklistEditor component created</done>
</task>

<task type="auto">
  <name>Task 5: TaskForm component</name>
  <files>frontend/components/tasks/TaskForm.tsx</files>
  <action>
    Create TaskForm.tsx — create/edit task form:
    - 'use client' directive
    - useActionState for form state (Next.js 16 native)
    - Fields:
      * Title (text input, required)
      * Description (textarea, optional)
      * Assignee (select dropdown — fetch family members from GET /api/admin/users or pass as prop)
      * Due date (date input, optional)
      * Checklist items (ChecklistEditor component)
    - Submit button with loading state
    - Error display per field
    - Props: onSubmit: (data: TaskRequest) => Promise<void>, initialValues?: Partial<TaskRequest>, submitLabel?: string
    - On successful submit: redirect to /tasks (use router.push)
  </action>
  <verify>grep "useActionState\|ChecklistEditor\|assignee" frontend/components/tasks/TaskForm.tsx</verify>
  <acceptance_criteria>
    - TaskForm uses useActionState
    - Includes all 5 fields including ChecklistEditor
    - Shows loading state during submission
  </acceptance_criteria>
  <done>TaskForm component created</done>
</task>

<task type="auto">
  <name>Task 6: Tasks list page</name>
  <files>frontend/app/(app)/tasks/page.tsx</files>
  <action>
    Create the tasks list page at frontend/app/(app)/tasks/page.tsx:
    - 'use client' directive
    - "Family Tasks" / "My Tasks" toggle tabs at top
    - Fetch tasks from getTasks() or getMyTasks() based on active tab
    - Render TaskCard grid (2 cols on desktop, 1 on mobile)
    - "New Task" button (→ /tasks/new) in top-right
    - Empty state: "No tasks yet — create the first one!" when list is empty
    - Loading skeleton while fetching
    - Group tasks: overdue first (red header), then by due date, then no due date
  </action>
  <verify>grep "Family Tasks\|My Tasks\|getTasks" frontend/app/\(app\)/tasks/page.tsx</verify>
  <acceptance_criteria>
    - Tasks page shows Family/My toggle
    - Renders TaskCards in a grid
    - Has "New Task" button
    - Shows empty state when no tasks
  </acceptance_criteria>
  <done>Tasks list page created</done>
</task>

<task type="auto">
  <name>Task 7: Task detail page</name>
  <files>frontend/app/(app)/tasks/[id]/page.tsx</files>
  <action>
    Create task detail page at frontend/app/(app)/tasks/[id]/page.tsx:
    - 'use client' directive
    - Fetch task by id using getTask(id)
    - Display:
      * Title (large, strikethrough if DONE)
      * Status badge + "Mark Complete" button (if not DONE)
      * Assignee with avatar
      * Due date (colored if overdue)
      * Description (full text)
      * Checklist items (list of ChecklistItem components, each toggleable)
      * Created by + created at
    - "Edit" button → /tasks/[id]/edit (placeholder for now)
    - "Delete" button (show confirm dialog) → deleteTask, redirect to /tasks
    - Back link → /tasks
  </action>
  <verify>grep "getTask\|ChecklistItem\|completeTask" frontend/app/\(app\)/tasks/\[id\]/page.tsx</verify>
  <acceptance_criteria>
    - Task detail shows all task fields
    - Checklist items are individually toggleable
    - "Mark Complete" button works
    - Delete with confirmation works
  </acceptance_criteria>
  <done>Task detail page created</done>
</task>

<task type="auto">
  <name>Task 8: New task page</name>
  <files>frontend/app/(app)/tasks/new/page.tsx</files>
  <action>
    Create frontend/app/(app)/tasks/new/page.tsx:
    - Simple page wrapping TaskForm
    - Page title "New Task"
    - Back link → /tasks
    - On submit: call createTask(data), redirect to /tasks on success
  </action>
  <verify>cat frontend/app/\(app\)/tasks/new/page.tsx</verify>
  <acceptance_criteria>
    - New task page renders TaskForm
    - Successful submit redirects to /tasks
  </acceptance_criteria>
  <done>New task page created</done>
</task>

</tasks>
