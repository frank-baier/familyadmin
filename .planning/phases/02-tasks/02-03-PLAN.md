---
phase: 02-tasks
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - backend/src/main/java/de/baier/familyadmin/service/NotificationService.java
  - backend/src/main/java/de/baier/familyadmin/scheduler/TaskReminderScheduler.java
  - backend/src/main/resources/templates/email/task-reminder.html
  - backend/pom.xml
autonomous: true
requirements:
  - Email reminders when tasks are due

must_haves:
  truths:
    - A daily scheduled job runs at 08:00 and sends email reminders for tasks due today or overdue
    - Emails are sent only to the assigned user
    - Email contains task title, due date, and a link to the task
    - No email sent for completed tasks
  artifacts:
    - backend/src/main/java/de/baier/familyadmin/scheduler/TaskReminderScheduler.java
    - backend/src/main/java/de/baier/familyadmin/service/NotificationService.java
    - backend/src/main/resources/templates/email/task-reminder.html
---

<objective>
Build email reminders for due tasks using Spring @Scheduled + JavaMailSender.

Purpose: Automated reminders keep the family on top of tasks without anyone having to check the app.
Output: Daily scheduler that sends HTML email reminders for tasks due today or overdue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Thymeleaf dependency for email templates</name>
  <files>backend/pom.xml</files>
  <read_first>backend/pom.xml</read_first>
  <action>
    Add spring-boot-starter-thymeleaf and spring-boot-starter-mail to pom.xml dependencies:
    ```xml
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-thymeleaf</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-mail</artifactId>
    </dependency>
    ```
  </action>
  <verify>grep "thymeleaf\|starter-mail" backend/pom.xml</verify>
  <acceptance_criteria>
    - pom.xml contains thymeleaf and mail starters
  </acceptance_criteria>
  <done>Dependencies added</done>
</task>

<task type="auto">
  <name>Task 2: HTML email template</name>
  <files>backend/src/main/resources/templates/email/task-reminder.html</files>
  <action>
    Create a clean Thymeleaf HTML email template:
    - Subject: "Reminder: [task title] is due [today/overdue]"
    - Body: greeting with user name, task title, due date, description snippet (max 100 chars)
    - "View Task" CTA button linking to ${appUrl}/tasks/${taskId}
    - Clean inline CSS (email clients don't support external CSS)
    - Thymeleaf variables: userName, taskTitle, dueDate, isOverdue, taskUrl

    ```html
    <!DOCTYPE html>
    <html xmlns:th="http://www.thymeleaf.org">
    <head><meta charset="UTF-8"/></head>
    <body style="font-family: Arial, sans-serif; max-width: 600px; margin: 0 auto; padding: 20px;">
      <h2 style="color: #4f46e5;">FamilyAdmin</h2>
      <p>Hi <span th:text="${userName}">Name</span>,</p>
      <p th:if="${isOverdue}">This task is <strong style="color:#dc2626;">overdue</strong>:</p>
      <p th:unless="${isOverdue}">This task is due <strong>today</strong>:</p>
      <div style="background:#f5f5f5; border-left:4px solid #4f46e5; padding:12px; margin:16px 0;">
        <strong th:text="${taskTitle}">Task Title</strong><br/>
        <small th:text="${dueDate}">2026-01-01</small>
      </div>
      <a th:href="${taskUrl}" style="background:#4f46e5;color:white;padding:10px 20px;text-decoration:none;border-radius:4px;">View Task</a>
    </body>
    </html>
    ```
  </action>
  <verify>cat backend/src/main/resources/templates/email/task-reminder.html | grep "thymeleaf"</verify>
  <acceptance_criteria>
    - Email template exists with Thymeleaf variables
    - Has task title, due date, and View Task link
  </acceptance_criteria>
  <done>Email template created</done>
</task>

<task type="auto">
  <name>Task 3: NotificationService</name>
  <files>backend/src/main/java/de/baier/familyadmin/service/NotificationService.java</files>
  <action>
    Create NotificationService.java with @Service @RequiredArgsConstructor:

    - Inject: JavaMailSender mailSender, SpringTemplateEngine templateEngine, @Value("${app.frontend.url}") String appUrl, @Value("${spring.mail.username}") String fromEmail

    - sendTaskReminder(Task task) — async method (@Async):
      * Skip if task.assignee is null or task.status == DONE
      * Build Thymeleaf context with: userName, taskTitle, dueDate, isOverdue (dueDate < today), taskUrl
      * Render template "email/task-reminder"
      * Send HTML email via MimeMessage to task.assignee.email
      * Log success/failure without throwing (don't break the scheduler)

    - Enable @EnableAsync on the application class or a config class
  </action>
  <verify>grep "sendTaskReminder\|JavaMailSender\|Async" backend/src/main/java/de/baier/familyadmin/service/NotificationService.java</verify>
  <acceptance_criteria>
    - NotificationService has async sendTaskReminder method
    - Skips completed tasks and tasks without assignee
  </acceptance_criteria>
  <done>NotificationService created</done>
</task>

<task type="auto">
  <name>Task 4: TaskReminderScheduler</name>
  <files>backend/src/main/java/de/baier/familyadmin/scheduler/TaskReminderScheduler.java</files>
  <action>
    Create TaskReminderScheduler.java with @Component @RequiredArgsConstructor:

    - Inject: TaskRepository taskRepository, NotificationService notificationService

    - @Scheduled(cron = "0 0 8 * * *") — runs daily at 08:00
    sendDailyReminders():
      * Find all tasks where status != DONE and dueDate <= today
      * For each task, call notificationService.sendTaskReminder(task)
      * Log: "Sending reminders for X tasks"

    Also add @EnableScheduling to FamilyAdminApplication.java.
  </action>
  <verify>grep "Scheduled\|sendDailyReminders" backend/src/main/java/de/baier/familyadmin/scheduler/TaskReminderScheduler.java</verify>
  <acceptance_criteria>
    - Scheduler runs daily at 08:00
    - Only sends reminders for non-done tasks with due date <= today
  </acceptance_criteria>
  <done>TaskReminderScheduler created</done>
</task>

<task type="auto">
  <name>Task 5: Enable Async and Scheduling</name>
  <files>backend/src/main/java/de/baier/familyadmin/FamilyAdminApplication.java</files>
  <read_first>backend/src/main/java/de/baier/familyadmin/FamilyAdminApplication.java</read_first>
  <action>
    Add @EnableScheduling and @EnableAsync to FamilyAdminApplication.java.
  </action>
  <verify>grep "EnableScheduling\|EnableAsync" backend/src/main/java/de/baier/familyadmin/FamilyAdminApplication.java</verify>
  <acceptance_criteria>
    - @EnableScheduling and @EnableAsync present on FamilyAdminApplication
  </acceptance_criteria>
  <done>Scheduling and Async enabled</done>
</task>

</tasks>
